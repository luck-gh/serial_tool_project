# Dialogs 模块

`dialogs` 模块包含应用程序中使用的所有自定义对话框。

## `comment_edit_dialog.py`

- **`CommentEditDialog`**: 一个简单的模态对话框，提供一个 `QTextEdit` 用于编辑命令的注释。

## `number_conversion_dialog.py`

- **`NumberConversionDialog`**: 一个非模态对话框，用于在不同的数字基（十六进制、十进制、二进制、八进制）之间进行实时转换。它还包括一个位键盘，用于直接操作和可视化单个位。
