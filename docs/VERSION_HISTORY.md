# 版本更新记录 (Version History)

本文档记录 GHowe 串口调试助手的所有版本更新历史。

---

## 版本 1.1.4 (2026-05-04)

### 摘要

```txt
优化左侧面板布局、模板导入导出体验与界面交互反馈

主要更新内容:
1. 新增行号跳转控件 - 支持直接输入行号并快速定位到目标命令
2. 新增模块同步按钮 - 支持跳转模块与发送模块双向同步
3. 收窄左侧面板布局 - 左侧下拉框与按钮更紧凑
4. 统一按钮交互反馈 - 瞬时按钮按下变色/释放恢复，执行型按钮运行中变色、结束后恢复
5. 增强模板导入导出可靠性 - 增加进度弹窗、支持取消、导入取消回滚、导出原子替换
6. 修复导入模板模块重建 - mode/modeend 行导入后可正确恢复模块结构
7. 增强 GUI 回归测试 - 覆盖跳行、按钮反馈、模板取消回滚、左侧宽度约束等场景

修改文件:
- main_window.py
- test/test_main_window_gui_regression.py
- main.py
- README.md

版本更新: 1.1.3 -> 1.1.4
```

### 功能优化

#### 左侧面板布局
- **优化** 左侧设置区布局
  - 端口、波特率、数据位、校验位、停止位下拉框宽度收窄
  - 左侧主要按钮宽度收窄
  - 左侧整体视觉更紧凑，节省界面空间

#### 行跳转与模块同步
- **新增** 行号跳转控件
  - 可直接输入行号并跳转到目标命令
- **新增** 模块同步按钮
  - 支持跳转模块与发送模块双向同步

### 模板导入导出改进
- **增强** 导入/导出进度弹窗
  - 导入导出过程显示进度
  - 支持取消操作
- **增强** 导入取消回滚
  - 取消后自动恢复导入前的命令状态
- **增强** 导出原子替换
  - 先写临时文件，再替换目标文件，避免取消时覆盖原文件
- **修复** 模块重建
  - `mode` / `modeend` 行导入后可正确恢复模块结构

### UI 交互改进
- **统一** 按钮反馈规则
  - 瞬时操作按钮：按下变色，释放恢复
  - 执行型按钮：开始执行变色，结束执行恢复

### 测试改进
- **新增** GUI 回归测试
  - 跳行控件
  - 左侧宽度约束
  - 按钮反馈
  - 模板取消与回滚
  - 模块重建

### 版本信息

- 工具版本: 1.1.3 → 1.1.4
- 配置版本: 1.1.3 → 1.1.4
- 更新时间: 2026-05-04

---

## 版本 1.1.3 (2026-02-02)

### 摘要

```txt
优化特殊指令系统和用户交互体验

主要更新内容:
1. 优化 BaudRate 动态切换 - 无需关闭/重开串口，直接修改波特率属性
2. 改进 BaudRate 对话框 - 使用可编辑下拉框，支持预设值和手动输入
3. 改进 SetEndlog 对话框 - 下拉选项显示正确的转义字符(\r\n、\r、\n)
4. 新增 modeend 指令 - 结束当前模块定义，支持连续发送时正确分隔模块
5. 修复 BaudRate/SetEndlog 连续发送 - 修复在连续发送中被跳过的问题
6. 优化按钮视觉反馈 - "<-"同步按钮点击时显示红色

修改文件:
- core/serial_thread.py
- main_window.py
- managers/special_command_manager.py
- widgets/command_widgets.py

版本更新: 1.1.2 -> 1.1.3
```

### 功能优化

#### BaudRate 动态切换
- **优化** 波特率切换机制
  - 原有: 切换波特率时需关闭串口再重新打开
  - 现在: 直接修改串口的 baudrate 属性，无需断开连接
  - 优势: 连续发送过程中切换波特率不会中断发送流程
  - 实现: `SerialThread.set_baudrate()` 方法

#### 特殊指令对话框改进
- **改进** BaudRate 对话框
  - 原有: 只能手动输入数字
  - 现在: 可编辑下拉框，提供常用波特率预设 (9600, 19200, 38400, 57600, 115200, 230400, 460800, 921600)
  - 优势: 快速选择常用值，同时支持自定义输入

- **改进** SetEndlog 对话框
  - 原有: 选项显示为 `rn`、`r`、`n`
  - 现在: 选项显示为 `\r\n`、`\r`、`\n`
  - 优势: 更直观，符合转义字符的标准表示

### 新增功能

#### modeend 指令
- **新增** modeend 结束模块定义指令
  - 格式: `modeend:0` 或 `modeend:`
  - 作用: 结束当前模块定义，后续命令归入"默认"模块
  - 连续发送时: 遇到 modeend 会正确结束当前模块的执行
  - 应用: 在复杂命令序列中明确分隔不同功能模块

### Bug 修复

#### 连续发送特殊指令修复
- **修复** BaudRate 和 SetEndlog 在连续发送中被跳过
  - 问题: `send_module_command` 循环只处理部分特殊指令类型
  - 修复: 添加 BAUDRATE 和 SETENDLOG 的显式处理
  - 效果: 所有特殊指令在连续发送中都能正常执行

### UI 优化

#### 按钮视觉反馈
- **优化** `<-` 同步按钮点击反馈
  - 原有: 点击时闪烁绿色
  - 现在: 点击时闪烁红色
  - 优势: 与其他蓝色按钮的点击反馈保持一致

### 修改的文件清单

1. **core/serial_thread.py**
   - 新增 `set_baudrate()` 方法

2. **main_window.py**
   - `update_baudrate()` - 改用动态波特率切换
   - `_sync_mode_selection()` - 按钮点击颜色改为红色

3. **managers/special_command_manager.py**
   - `_handle_mode_end()` - 实现停止逻辑
   - `send_module_command()` - 添加 BAUDRATE/SETENDLOG 处理

4. **widgets/command_widgets.py**
   - `add_special_command()` - 改进 BaudRate 和 SetEndlog 对话框

### 版本信息

- 工具版本: 1.1.2 → 1.1.3
- 配置版本: 1.1.2 → 1.1.3
- 更新时间: 2026-02-02

---

## 版本 1.1.2 (2026-01-24)

### 摘要

```txt
修复CSV处理问题并新增StopContinuous流程控制指令

主要修复内容:
1. 修复CSV导入导出双引号累积问题 - 使用标准csv.reader/writer，符合RFC 4180
2. 修复CSV导入BOM字符识别失败 - 改用utf-8-sig编码自动处理BOM
3. 新增StopContinuous特殊指令 - 支持提前结束本轮或完全停止
4. 增强按钮可视化反馈 - 为配置、复位、保存、清空、导入、导出等按钮添加闪烁效果
5. 优化多列注释支持 - CSV第3列及之后的所有内容自动合并为注释

修改文件:
- main_window.py
- managers/special_command_manager.py
- widgets/command_widgets.py
- utils/ui_utils.py

版本更新: 1.1.1 -> 1.1.2
```

### Bug 修复

#### CSV 导入导出问题

**问题1：双引号累积**
- **现象**: 导出导入循环后，注释字段中的双引号不断累积
- **根本原因**:
  - 导出时：csv.writer 自动添加引号，同时手动转义导致双重转义
  - 导入时：使用手动字符串分割 `split(',')` 无法正确处理 CSV 标准引号
- **解决方案**:
  - 导出：显式指定 `quoting=csv.QUOTE_MINIMAL`，让 Python csv 模块自动处理引号
  - 导入：使用 `csv.reader()` 替代手动分割，自动处理 CSV 标准转义
- **参考文档**: [BUG_FIX_CSV_QUOTE_ACCUMULATION.md](BUG_FIX_CSV_QUOTE_ACCUMULATION.md)

**问题2：BOM 字符导致第一行无法识别**
- **现象**: 部分 CSV 文件（如 RxCH1+RxCH2+TxCH1+TxCH2.csv）第一行无法识别
- **根本原因**: 文件包含 UTF-8 BOM 字符 (0xEF 0xBB 0xBF)，导致第一列被解析为 "﻿False" 而非 "False"
- **解决方案**: 将 `encoding='utf-8'` 改为 `encoding='utf-8-sig'`，自动移除 BOM
- **位置**: [main_window.py:1353](../main_window.py#L1353)

**问题3：多列注释解析**
- **增强**: 现在支持第3列及之后的所有列都作为注释（用逗号连接）
- **好处**: 用户可以在注释中使用任意数量的逗号，不会被当作列分隔符
- **示例**:
  ```csv
  True,AT+CONFIG,配置参数,波特率9600,数据位8,校验位None
  ```
  注释将被正确解析为：`"配置参数,波特率9600,数据位8,校验位None"`

#### StopContinuous 指令问题

**问题1：StopContinuous 在普通连续发送中不生效**
- **现象**: 执行 StopContinuous 指令后无系统消息，无法停止
- **根本原因**: `send_continuous_commands()` 的 `send_next_command` 函数未处理 STOPCONTINUOUS 类型
- **解决方案**: 在 [main_window.py:1173-1179](../main_window.py#L1173-L1179) 添加 StopContinuous 处理逻辑
- **效果**: 现在 StopContinuous 在普通连续发送和 SendMode 中都能正常工作

**问题2：StopContinuous:0 行为不正确**
- **期望行为**: 提前结束本轮，但继续循环（等待循环间隔后自动开始下一轮）
- **实际行为**: 完全停止了连续发送，不再继续循环
- **根本原因**:
  - 使用 `_force_no_loop` 标志但在闭包中被捕获，中途设置不生效
  - 调用 `stop_continuous_send()` 导致完全停止
- **解决方案**:
  - 模式0：设置 `_skip_to_loop` 标志，不调用 `stop_continuous_send()`
  - 在 `send_next_command` 开始时检查标志，立即跳转到循环检查
  - 简化循环逻辑，移除 `_force_no_loop` 标志
- **位置**:
  - [managers/special_command_manager.py:95-128](../managers/special_command_manager.py#L95-L128)
  - [main_window.py:1110-1124](../main_window.py#L1110-L1124)

### 新增功能

#### StopContinuous 特殊指令

**功能**: 控制连续发送流程，可选择提前结束本轮或完全停止

**格式**: `StopContinuous:参数`

**参数说明**:
- `0` 或 空 - 提前结束本轮，继续循环（默认）
  - 停止执行当前轮的后续命令
  - 保持循环发送勾选状态
  - 等待循环间隔后，自动开始下一轮
  - 适用场景：提前结束本轮测试，立即进入下一轮循环

- `1` - 完全停止连续发送和循环发送
  - 停止执行当前轮的后续命令
  - 取消循环发送勾选
  - 完全停止，不再继续
  - 适用场景：完全停止所有自动发送功能

**系统消息**:
- 参数为 0: `"StopContinuous: 提前结束本轮发送，等待下一轮循环"`
- 参数为 1: `"StopContinuous: 已停止连续发送和循环发送"`

**应用场景**:
```
命令序列示例（勾选了"循环发送"）：
第一轮:
1. [✓] AT+TEST1              ← 执行
2. [✓] AT+TEST2              ← 执行
3. [✓] StopContinuous:0      ← 执行，提前结束本轮
4. [✓] AT+TEST3              ← 不执行（已跳过）

等待循环间隔...

第二轮:
1. [✓] AT+TEST1              ← 执行
2. [✓] AT+TEST2              ← 执行
3. [✓] StopContinuous:0      ← 执行，提前结束本轮
4. [✓] AT+TEST3              ← 不执行（已跳过）

...（继续循环）
```

**参考文档**: [ENHANCEMENT_STOP_COMMANDS.md](ENHANCEMENT_STOP_COMMANDS.md)

#### 按钮可视化反馈增强

**第一批（蓝色按钮）**:
- ✅ 配置按钮 - 点击时闪烁红色 200ms
- ✅ 跳转到此按钮 - 点击时闪烁红色 200ms
- ✅ 复位按钮 - 点击时闪烁红色 200ms + 系统消息 "统计数据已复位"

**第二批（绿色按钮）**:
- ✅ 保存数据按钮 - 点击时闪烁红色 200ms
- ✅ 清空数据按钮 - 点击时闪烁红色 200ms + 系统消息 "接收数据已清空"
- ✅ 导入模板按钮 - 点击时闪烁红色 200ms
- ✅ 导出模板按钮 - 点击时闪烁红色 200ms

**参考文档**: [ENHANCEMENT_VISUAL_FEEDBACK.md](ENHANCEMENT_VISUAL_FEEDBACK.md)

### 技术改进

#### CSV 处理优化
- **标准兼容**: 生成的 CSV 文件符合 RFC 4180 标准
- **工具兼容**: 可被 Excel、LibreOffice 等标准 CSV 工具正确打开
- **向后兼容**: 新代码可以正确读取旧格式的 CSV 文件
- **多列支持**: 第3列及之后的所有内容自动合并为注释，支持注释中包含逗号

#### 流程控制增强
- **_skip_to_loop 标志**: 新增标志用于控制提前结束本轮但继续循环
- **动态循环检查**: 在 `send_next_command` 开始时动态检查标志
- **简化循环逻辑**: 移除 `_force_no_loop` 复杂逻辑，使用更直观的 `_skip_to_loop`

### 测试验证

#### CSV 测试
提供了两个测试脚本验证修复：

1. **test_csv_export_import.py** - 导出导入循环测试
   - 测试包含各种特殊字符的注释（逗号、双引号、制表符、换行符）
   - 进行3轮导出导入循环
   - 验证数据完整性，确认双引号不再累积

2. **test_multi_column_comment.py** - 多列注释测试
   - 验证第3列及之后的所有内容都被合并为注释
   - 测试手动创建的多列 CSV 文件
   - 确认逗号不会被当作列分隔符

**测试结果**: 所有测试通过 ✅

### 修改的文件清单

1. **main_window.py**
   - `import_template()` - 改用 utf-8-sig 编码，使用 csv.reader
   - `export_template()` - 显式指定 csv.QUOTE_MINIMAL
   - `send_continuous_commands()` - 添加 StopContinuous 处理，优化循环逻辑
   - `open_config_dialog()` - 添加配置按钮闪烁效果
   - `reset_statistics()` - 添加复位按钮闪烁效果和系统消息
   - `_jump_to_module_row()` - 添加跳转按钮闪烁效果
   - `save_receive_data()` - 添加保存数据按钮闪烁效果
   - `clear_receive_data()` - 添加清空数据按钮闪烁效果和系统消息

2. **managers/special_command_manager.py**
   - `_handle_stop_continuous()` - 重写逻辑，支持模式0和模式1
   - `execute_sendmode_inline()` - 更新 StopContinuous 处理

3. **widgets/command_widgets.py**
   - `add_special_command()` - 添加 StopContinuous 参数配置对话框

4. **utils/ui_utils.py**
   - 移除 STOPLOOP 枚举（已合并到 STOPCONTINUOUS）

### 兼容性说明

- **向后兼容**: 所有修改都向后兼容，不影响现有功能
- **CSV 兼容**: 新代码可以正确读取旧格式的 CSV 文件
- **配置兼容**: 配置文件格式未改变，无需升级

### 版本信息

- 工具版本: 1.1.1 → 1.1.2
- 配置版本: 1.1.1 → 1.1.2
- 更新时间: 2026-01-24

---

## 版本 1.1.1 (2026-01-23)

### 摘要

```txt
修复固件下载工具串口参数传递和窗口置顶问题

主要修复内容:
1. 修复内部工具始终置顶设置 - apply_always_on_top_setting始终调用show()
2. 延长串口关闭等待时间 - 从3秒增加到5秒+500ms额外延迟，解决"信号灯超时"错误
3. 修复串口配置获取时机 - 在关闭串口前获取配置，避免serial_thread为None
4. 修复内部工具参数应用 - 在apply_initial_config中添加bytesize/parity/stopbits的UI设置
5. 添加外部工具完整参数传递 - 传递40+配置参数（串口、ACK、CRC等）
6. 添加校验位格式转换映射 - 'N'/'O'/'E' <-> "None"/"Odd"/"Even"

修改文件:
- serial_tool_project/widgets/base_widgets.py
- firmware_downloader_project/firmware_downloader_dialog.py

版本更新: 1.1.0 -> 1.1.1
```

### 关键 Bug 修复

#### 工具窗口置顶问题
- **修复** 内部工具始终置顶失效问题
  - 问题: `apply_always_on_top_setting()` 只在窗口标志改变时调用 `show()`，导致窗口可能不显示
  - 修复: 无论标志是否改变，都调用 `show()` 确保窗口正常显示
  - 影响模块: `base_widgets.py:11-38`
- **说明** 外部工具置顶限制
  - 外部工具通过 `subprocess.Popen()` 启动，无法在父进程中控制子进程窗口标志
  - `always_on_top` 配置仅对内置工具有效 (操作系统限制)

#### 串口配置传递问题
- **修复** 串口参数传递不完整
  - 问题: 数据位(bytesize)、校验位(parity)、停止位(stopbits) 未传递到下载工具
  - 根源: `firmware_downloader_dialog.py` 的 `apply_initial_config()` 方法只设置端口号和波特率
  - 修复: 添加数据位、校验位、停止位的 UI 应用逻辑 (`firmware_downloader_dialog.py:783-800`)
  - 技术细节: 校验位需要从单字符格式('N', 'O', 'E', 'M', 'S')转换为 UI 显示格式("None", "Odd", "Even", "Mark", "Space")

- **修复** 串口配置获取时机错误
  - 问题: 先调用 `prepare_tool_launch()` 关闭串口，后获取配置，导致 `serial_thread` 已为 `None`
  - 修复: 将串口配置获取逻辑移到 `prepare_tool_launch()` 调用之前 (`base_widgets.py:310-393`)
  - 影响: 确保能正确获取当前串口的所有参数 (port, baudrate, bytesize, parity, stopbits)

#### 串口资源释放问题
- **修复** 串口关闭时序问题
  - 问题: 串口关闭后立即打开工具，出现"信号灯超时时间已到"错误
  - 根源: 系统需要时间释放串口句柄，立即打开导致资源冲突
  - 修复: 延长等待时间并优化等待逻辑 (`base_widgets.py:57-67`)
    - 等待串口线程完全停止: 从 3 秒增加到 5 秒 (每 100ms 检查一次)
    - 额外等待系统释放句柄: 增加 500ms 固定等待
    - 使用 `QApplication.processEvents()` 确保事件循环处理

#### 外部工具参数传递增强
- **新增** 外部工具完整参数传递
  - 扩展传递参数: 从原来 5 个参数扩展到 40+ 个配置参数
  - 新增传递内容:
    - 基础串口配置 (port, baudrate, bytesize, parity, stopbits)
    - 初始文件 (`--file`)
    - 下载基本配置 (`--packet-size`, `--start-command`)
    - 数据包 CRC 配置 (`--add-packet-crc`, `--packet-crc-type`)
    - 开始命令 ACK 配置 (8 个参数)
    - 数据包 ACK 配置 (9 个参数)
    - 末尾数据包 ACK 配置 (9 个参数)
    - 结尾字符串配置 (2 个参数)
  - 修复位置: `base_widgets.py:362-469`
  - 命令行格式: 使用 `--param value` 格式，布尔型使用 `--flag` 格式

### 技术改进

#### 参数格式转换
- **优化** pyserial 常量转换
  - 校验位: 内部格式('N') ↔ 显示格式("None")
  - 自动映射: `{'N': 'None', 'O': 'Odd', 'E': 'Even', 'M': 'Mark', 'S': 'Space'}`

#### 执行时序优化
- **改进** 串口配置获取逻辑
  - 确保在关闭串口前获取所有配置
  - 支持从串口线程或 UI 控件两种方式获取

#### 代码清理
- **移除** 调试输出语句
  - 清理开发期间的 print 语句
  - 提升生产环境代码质量

### 影响范围

#### 修改的文件
- `serial_tool_project/widgets/base_widgets.py` - 核心修复
- `firmware_downloader_project/firmware_downloader_dialog.py` - 参数应用修复

#### 配置版本
- 工具版本: 1.1.0 → 1.1.1
- 配置版本: 1.1.0 → 1.1.1
- 更新时间: 2026-01-23 10:57:53

---

## 版本 1.1.0 (2025-12-02)

### 摘要

```txt
重构工具注册表机制并优化SendMode执行顺序

主要更新内容:
1. 新增集中式工具注册表 - 统一管理所有工具元数据和默认配置
2. 修复SendMode执行顺序 - 严格按照定义顺序执行模块指令
3. 实现单一数据源原则 - 消除配置重复定义，自动同步
4. 重构代码架构 - 移除150+行重复代码，提升可维护性
5. 增强扩展性 - 新增工具只需在注册表中配置即可

修改文件:
- managers/config_manager.py
- main_window.py
- managers/special_command_manager.py

版本更新: 1.0.0 -> 1.1.0
```

### 核心优化

#### 工具注册表机制
- **新增** 集中式工具注册表
  - 实现: `ConfigManager.REGISTERED_TOOLS` 统一管理所有工具元数据
  - 优势: 默认配置集中定义，避免分散和不一致
  - 影响: 所有工具的元数据和默认配置统一管理
- **改进** SendMode 指令执行顺序
  - 问题: 原有实现可能导致模块执行顺序不可预测
  - 修复: 严格按照定义顺序执行模块指令
  - 技术: 使用队列机制确保执行顺序
- **重构** 代码优化
  - 移除: 150+ 行重复代码
  - 提升: 代码可维护性和扩展性

#### 配置管理优化
- **优化** 单一数据源原则
  - 实现: 默认配置集中定义在注册表中
  - 同步: 修改注册表默认值后，所有引用处自动同步
  - 易用: 新增工具时无需修改多处代码，只需在注册表中注册

### 技术改进

#### 架构设计
- **优化** DRY (Don't Repeat Yourself) 原则
  - 消除重复代码
  - 提高代码复用率
  - 降低维护成本
- **增强** 扩展性
  - 工具注册机制更易于扩展
  - 添加新工具只需简单配置
  - 支持插件化架构
- **改进** 一致性
  - 确保所有配置来源统一
  - 避免配置不一致导致的问题
  - 统一的错误处理机制

### 影响范围

#### 修改的文件
- `managers/config_manager.py` - 工具注册表实现
- `main_window.py` - 工具方法映射更新
- `managers/special_command_manager.py` - SendMode 执行逻辑优化

#### 配置版本
- 工具版本: 1.0.0 → 1.1.0
- 配置版本: 1.0.0 → 1.1.0
- 更新时间: 2025-12-02 00:00:00

---

## 版本 1.0.0 (2025-12-13)

### 摘要

```txt
正式发布V1.0版本 - 完整的串口调试工具

主要功能:
1. 实现完整串口通信功能 - 支持标准串口参数配置和异步收发
2. 实现命令管理系统 - 支持命令表格管理、模块化组织
3. 实现自动保存与恢复 - 状态持久化，启动自动恢复
4. 实现基础特殊指令 - delay和mode指令
5. 建立模块化架构 - core/managers/widgets/utils/dialogs
6. 实现PyQt5用户界面 - Fusion主题和自定义控件

发布内容:
- 完整源代码
- 基础文档(README)
- 打包配置(main.spec)

版本更新: 0.9.0 -> 1.0.0
```

### 重大里程碑

正式发布 V1.0 版本，标志着项目进入稳定阶段。

### 核心功能

#### 串口调试功能
- **新增** 完整的串口通信功能
  - 支持标准串口参数配置 (波特率、数据位、校验位、停止位)
  - 异步数据收发，UI 始终响应
  - 自动检测可用串口设备
- **实现** 基础特殊指令
  - delay 指令: 非阻塞延迟
  - mode 指令: 命令模块化管理

#### 命令管理系统
- **新增** 命令表格管理
  - 支持添加、编辑、删除命令
  - 命令勾选控制发送
  - 注释功能便于维护
- **实现** 模块化组织
  - 使用 mode 指令定义模块
  - 模块间快速切换
  - 独立管理不同功能模块

#### 自动保存与恢复
- **新增** 状态持久化
  - 自动保存所有命令和配置
  - 启动时自动恢复上次状态
  - JSON 格式配置文件
- **实现** 配置管理器
  - 集中管理应用配置
  - 版本检测和兼容性处理

### 技术实现

#### 架构设计
- **实现** 模块化架构
  - core: 核心功能 (串口线程)
  - managers: 功能管理器
  - widgets: 自定义控件
  - utils: 工具类和枚举
  - dialogs: 交互对话框

#### 用户界面
- **实现** PyQt5 GUI
  - Fusion 样式主题
  - 可折叠分组框
  - 自定义文本浏览器
  - 命令表格控件

### 影响范围

#### 发布内容
- 完整的源代码
- 基础文档 (README)
- 打包配置 (main.spec)

#### 配置版本
- 工具版本: 0.9.0 → 1.0.0
- 配置版本: 0.9.0 → 1.0.0
- 更新时间: 2025-12-13 00:00:00

---

## 版本 0.9.0 (2025-12-31)

### 摘要

```txt
新增特殊指令系统和输出过滤功能

主要更新内容:
1. 新增SendHex指令 - 十六进制发送，支持自动格式化和实时验证
2. 新增BaudRate指令 - 运行时动态切换波特率
3. 新增SetEndlog指令 - 动态修改发送结尾符
4. 新增SendMode指令 - 循环发送中临时跳转到指定模块
5. 新增输出来源过滤 - 四种日志分类（发送/接收/系统/错误）独立控制
6. 新增批量操作功能 - 多行同步勾选、批量插入/删除
7. 新增Ctrl+H快捷键 - 快速唤醒进制转换器

修改文件:
- managers/special_command_manager.py
- managers/output_manager.py
- widgets/command_widgets.py
- main_window.py

版本更新: 0.8.0 -> 0.9.0
```

### 功能增强

#### 特殊指令系统扩展
- **新增** SendHex 指令 - 十六进制发送
  - 功能: 直接发送原始十六进制字节数据
  - 格式化: 输入时自动添加空格，提高可读性
  - 过滤: 自动过滤非法字符 (只保留 0-9, A-F)
  - 优化: 光标位置智能修正
  - 应用: 二进制协议、寄存器操作、固件升级

- **新增** BaudRate 指令 - 波特率动态切换
  - 功能: 运行时动态切换串口波特率
  - 流程: 断开 → 修改波特率 → 重新连接
  - 机制: 自动重连机制
  - 应用: 自适应波特率调试

- **新增** SetEndlog 指令 - 结尾符设置
  - 功能: 动态修改发送命令的行尾标识符
  - 支持: `None`, `\r\n`, `\r`, `\n` 等多种结尾符
  - 应用: 适配不同设备的结尾符要求

- **新增** SendMode 指令 - 模块跳转
  - 功能: 循环发送过程中临时跳转到指定模块
  - 执行: 暂停当前模块 → 执行目标模块 → 返回原模块
  - 应用: 定期执行检测任务、条件性触发操作

#### 交互体验优化
- **改进** 特殊指令配置方式
  - 实现: 右键菜单交互式配置参数
  - 优势: 参数输入更加直观友好
  - 提示: 实时验证参数有效性

### 输出管理系统

#### 输出来源分类
- **新增** 四种输出来源过滤机制
  - 发送 (蓝色): 显示所有发送的命令
  - 接收 (黑色): 显示设备返回的数据
  - 系统 (灰色): 显示工具的系统消息
  - 错误 (红色): 显示错误和异常信息

#### 过滤控制功能
- **新增** 输出来源勾选功能
  - 实现: 独立勾选框控制各类日志显示
  - 优势: 可单独控制各类日志的显示/隐藏
  - 应用: 快速定位问题所在，减少干扰信息

### 批量操作增强

#### 多行操作功能
- **新增** 多行同步勾选
  - 功能: 多行选中时同步勾选连续发送状态
  - 支持: Shift 键连续选择、Ctrl 键多选
  - 应用: 快速勾选一组相关命令

- **新增** 批量插入行
  - 功能: 右键菜单快速插入指定数量的空行
  - 应用: 为新的测试模块预留空间

- **新增** 批量删除行
  - 功能: 支持一次性删除多个选中行
  - 应用: 快速清理无用命令

### 快捷键系统

#### 全局快捷键
- **新增** Ctrl+H 快捷键
  - 功能: 选中数值后快速唤醒进制转换器
  - 识别: 支持自动识别选中内容格式
  - 应用: 快速进制转换，提升调试效率

### 格式化功能

#### SendHex 实时格式化
- **新增** 十六进制自动格式化
  - 空格: 输入时自动添加空格分隔
  - 过滤: 自动过滤非法字符
  - 光标: 光标位置智能修正
  - 体验: 提升十六进制输入体验

### 影响范围

#### 修改的文件
- `managers/special_command_manager.py` - 新增 4 种特殊指令
- `managers/output_manager.py` - 输出过滤系统
- `widgets/command_widgets.py` - 批量操作功能
- `main_window.py` - 快捷键集成

#### 配置版本
- 工具版本: 0.8.0 → 0.9.0
- 配置版本: 0.8.0 → 0.9.0
- 更新时间: 2025-12-31 00:00:00

---

## 版本 0.8.0 (2025-12-30)

### 摘要

```txt
新增循环发送和工具集成系统

主要更新内容:
1. 新增循环发送功能 - 支持自动化循环执行命令序列，可自定义间隔
2. 改进连续发送功能 - 优化按固定时间间隔重复发送选中命令
3. 新增回车发送功能 - 编辑框支持Enter键快速发送指令
4. 新增自动保存与恢复 - 程序关闭时自动保存，启动时自动恢复
5. 新增工具按钮区域 - 左侧新增工具按钮，支持快速启动外部工具
6. 新增多选勾选功能 - 选中多个命令时可同时勾选连续发送状态
7. 新增内置进制转换器 - 集成位计算器，支持快捷键唤醒

修改文件:
- main_window.py
- managers/config_manager.py
- widgets/command_widgets.py

版本更新: 0.7.0 -> 0.8.0
```

### 自动化发送功能

#### 循环发送机制
- **新增** 循环发送功能
  - 功能: 支持自动化循环执行命令序列
  - 间隔: 可自定义循环间隔 (1ms - 9999ms)
  - 流程: 发送一轮 → 等待循环间隔 → 重新开始
  - 应用: 完整工作流程验证、周期性测试批次

#### 连续发送优化
- **改进** 连续发送功能
  - 功能: 按固定时间间隔重复发送选中命令
  - 间隔: 可自定义连续间隔 (1ms - 9999ms)
  - 控制: 点击停止按钮终止发送
  - 应用: 定时状态查询、持续性压力测试

### 用户体验优化

#### 输入增强
- **新增** 回车发送功能
  - 功能: 编辑框支持 Enter 键快速发送指令
  - 优势: 提升操作效率，减少鼠标操作
  - 应用: 快速调试，连续发送多条命令

#### 状态持久化
- **新增** 自动保存与恢复
  - 保存: 程序关闭时自动保存所有命令
  - 配置: 自动保存配置及界面布局
  - 恢复: 启动时自动恢复上次状态
  - 优势: 无缝工作体验，断点续调

### 工具集成系统

#### 外部工具支持
- **新增** 工具按钮区域
  - 位置: 左侧新增工具按钮区域
  - 功能: 支持快速启动外部工具
  - 配置: 可在设置中配置工具路径

#### 批量操作增强
- **新增** 多选勾选功能
  - 功能: 选中多个连续发送区命令时可同时勾选
  - 支持: Shift 键连续选择、Ctrl 键多选
  - 应用: 快速配置命令发送状态

#### 内置工具
- **新增** 内置进制转换器
  - 集成: 将位计算器集成到工具中
  - 灵活: 支持指定外部位计算器路径
  - 快捷: 提供快捷键唤醒 (Ctrl+H)

### 影响范围

#### 修改的文件
- `main_window.py` - 工具按钮和快捷键
- `managers/config_manager.py` - 自动保存机制
- `widgets/command_widgets.py` - 批量勾选功能

#### 配置版本
- 工具版本: 0.7.0 → 0.8.0
- 配置版本: 0.7.0 → 0.8.0
- 更新时间: 2025-12-30 00:00:00

---

## 版本 0.7.0 (2025-12-11)

### 摘要

```txt
新增目录记忆和命令模块跳转功能

主要更新内容:
1. 新增最后使用目录记录 - 导入导出文件时自动定位到上次使用的目录
2. 新增命令模块跳转功能 - 使用mode:指令定义命令模块，支持模块间快速切换
3. 实现配置文件持久化 - 目录路径自动保存到配置文件
4. 优化文件操作效率 - 减少目录切换次数，提升用户体验

修改文件:
- managers/config_manager.py
- managers/special_command_manager.py
- main_window.py

版本更新: 0.6.0 -> 0.7.0
```

### 文件管理增强

#### 目录记忆功能
- **新增** 最后使用目录记录
  - 功能: 记录最后一次使用的目录
  - 行为: 导入导出文件时自动定位到上次使用的目录
  - 优势: 提升文件操作效率，减少目录切换
  - 实现: 配置文件持久化存储

### 命令模块化系统

#### 模块跳转功能
- **新增** 命令模块跳转
  - 指令: 使用 `mode:` 指令定义命令模块
  - 切换: 支持在模块间快速切换
  - 组织: 模块化组织相关命令
  - 应用: 按功能分组管理复杂命令序列

### 影响范围

#### 修改的文件
- `managers/config_manager.py` - 目录记忆实现
- `managers/special_command_manager.py` - mode 指令实现
- `main_window.py` - 模块切换 UI

#### 配置版本
- 工具版本: 0.6.0 → 0.7.0
- 配置版本: 0.6.0 → 0.7.0
- 更新时间: 2025-12-11 00:00:00

---

## 版本 0.6.0 (2025-12-03)

### 摘要

```txt
修复字母显示不全问题，提升跨平台兼容性

主要修复内容:
1. 修复字母显示不全问题 - 解决不同环境下字母显示不完整的问题
2. 优化字体渲染机制 - 改进字体渲染算法，确保文字正确显示
3. 改进界面布局适配 - 优化布局算法，提升在不同分辨率下的显示效果
4. 提升跨平台兼容性 - 确保在Windows/Linux/macOS上显示一致

修改文件:
- utils/ui_utils.py
- main_window.py

版本更新: 0.5.0 -> 0.6.0
```

### 界面优化

#### 显示兼容性修复
- **修复** 字母显示不全问题
  - 问题: 在不同环境下字母显示不全
  - 根源: 字体渲染和布局适配问题
  - 修复: 优化字体渲染机制
  - 改进: 改进界面布局适配算法
  - 效果: 提升跨平台兼容性 (Windows/Linux/macOS)

### 影响范围

#### 修改的文件
- `utils/ui_utils.py` - 字体渲染优化
- `main_window.py` - 布局适配改进

#### 配置版本
- 工具版本: 0.5.0 → 0.6.0
- 配置版本: 0.5.0 → 0.6.0
- 更新时间: 2025-12-03 00:00:00

---

## 早期版本 (0.1.0 - 0.5.0)

### 核心功能建立

#### 基础架构
- **实现** 基础串口通信功能
  - pyserial 集成
  - 异步收发线程
  - 串口参数配置

#### 命令管理
- **实现** 命令表格管理
  - 添加、编辑、删除命令
  - 命令勾选控制
  - 注释功能

#### 特殊指令
- **实现** 特殊指令系统基础
  - delay 指令: 非阻塞延迟
  - 指令解析器架构

#### 输出显示
- **实现** 接收区显示优化
  - 颜色分类显示
  - 时间戳支持
  - 自动滚动

#### 文件操作
- **实现** 文件导入导出
  - CSV 格式支持
  - TXT 格式支持
  - 模板管理

#### 配置管理
- **实现** 配置管理基础
  - JSON 配置文件
  - 自动保存机制

#### 用户界面
- **实现** UI 风格设计
  - Fusion 样式
  - 自定义控件库
  - 响应式布局

### 技术积累
- PyQt5 GUI 框架搭建
- 多线程架构设计
- 配置持久化机制
- 模块化代码组织

---

## 版本号说明

版本号格式: `主版本.次版本.修订版本`

- **主版本**: 重大架构变更或功能重构
- **次版本**: 新增功能、特性增强
- **修订版本**: Bug 修复、小优化

---

## 升级建议

### 从 1.0.x 升级到 1.1.x
- 自动兼容，无需手动迁移
- 配置文件自动升级
- 建议备份配置文件后升级

### 从 0.x 升级到 1.x
- 配置文件格式已变更
- 建议导出命令模板后重新配置
- 工具路径需要重新设置

---

## 未来规划

### 计划中的功能
- 脚本录制与回放
- 数据可视化图表
- 协议解析插件系统
- 多串口同时监控
- 命令宏定义

---

<p align="center">
  <i>持续更新中...</i>
</p>
